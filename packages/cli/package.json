{
  "name": "colsidx",
  "version": "1.1.12",
  "description": "",
  "publishConfig": {
    "access": "public"
  },
  "bin": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "tsc --watch --preserveWatchOutput",
    "prepublishOnly": "esbuild src/index.ts --platform=node --outfile=dist/index.js --bundle --minify --define:process.env.NODE_ENV=\\\"production\\\" && npm run copy-files",
    "copy-files": "rsync -u dist/idxFromScratch.js dist/idxstarter.js dist/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/colson0x1/idx.git"
  },
  "author": "Colson",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/colson0x1/idx/issues"
  },
  "homepage": "https://github.com/colson0x1/idx#readme",
  "dependencies": {
    "@colsidx/local-client": "^1.0.0"
  },
  "devDependencies": {
    "@colsidx/local-api": "^1.0.0",
    "@types/node": "^20.9.0",
    "commander": "^11.1.0",
    "esbuild": "0.8.26",
    "typescript": "^5.2.2"
  }
}
